<h1 class="text-center">Campaigns</h1>
<div class="col-md-3 text-center">
  <h3>Number of Campaigns: &nbsp;<strong><%= @campaign_count %></strong></h3>
</div>
<div class="col-md-6">
    <hr>
    <% @user_campaigns.each do |campaign| %>
        <h3 class="text-center"><%=link_to campaign.name, campaign, style: "font-size: 2rem;" %><h3>
    <% end %>
</div>
